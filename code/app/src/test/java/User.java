// Interface that describes the app's Users. Implemented by the Customer.
public interface User {

    public String getId();			// Return the User's ID (username or employee number).
    public String getPassword();	// Return the User's password.
}
